# Exercise: Defining Custom Annotations

This is a pretty simple exercise where a custom annotation is defined


## Compile

There's no `main()` method to run — just make sure everything compiles:

    javac -cp . *.java